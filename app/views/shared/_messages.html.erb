<div class="row">
  <div class="col-md-12">
  	<% flash.each do |name, msg| %>
    <div class='alert <%= bootstrap_class_for(name) %> %>' role='alert'>
		<a href="#" class="close" data-dismiss="alert">×</a>
		<div class="fa <%= bootstrap_glyphs_icon(name)%>"><%= msg %></div>
	</div>
    <% end %>
  </div>
</div>
